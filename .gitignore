node_modules/
dist/
.next/
out/
.env
.env.local
.env*.local
.DS_Store
._*
*.log
*.tsbuildinfo
.superpowers/

# Local-only / generated / not for VCS
designbridge-dev/
Testdaten/
Artefakte/
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/skills/
.claude/settings.local.json
exports/
**/public/data/generated/
# stale root orchestrator manifests (real ones live in designbridge-web/ etc.)
/package.json
/package-lock.json
.worktrees/
# Grounding-Verifikations-Harness: flüchtige Bundle-/tmp-Verzeichnisse (Scheibe 1 Schritt 6)
web/.verify-*/

# Brainstorm-Companion (Mockups, Sessions)
.superpowers/

# Generat des Messwerkzeugs (web/verification/measure-natural-widths.mjs) — jederzeit neu baubar
web/verification/natural-widths.html
# Arbeitskopie der Rohdaten für emit-in-browser.html (Vite serviert nur unterhalb von web/).
# Original: storybook-harness/fixtures/prod-scan-raw.json — die Kopie nie committen, sie veraltet still.
web/verification/prod-scan-raw.json
# Generat aus dem Plugin-Writer (npm run bundle-build-order) fuer build-order-in-browser.html
web/verification/buildOrder.mjs
