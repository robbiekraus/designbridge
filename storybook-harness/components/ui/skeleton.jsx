// API-kompatibler Stub (kein Radix) — Klassen wie shadcn/ui new-york.
export function Skeleton({ className = '', ...props }) {
  return <div className={`animate-pulse rounded-md bg-muted ${className}`.trim()} {...props} />;
}
